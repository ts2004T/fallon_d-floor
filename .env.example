# PostgreSQL connection used by api/main.py
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fallon_dfloor
DB_USER=postgres
DB_PASSWORD=changeme

# Comma-separated list of origins allowed to call this API (CORS).
# Include the deployed frontend URL(s) in production, e.g.:
# ALLOWED_ORIGINS=http://localhost:3000,https://fallon-dfloor.example.com
ALLOWED_ORIGINS=http://localhost:3000
